/**
 * Workspace actions — re-export module for frontend compatibility.
 */

export {
  createWorkspace,
  updateWorkspace,
  inviteMember,
  acceptInvite,
  changeMemberRole,
  removeMember,
} from "./workspace-actions";
