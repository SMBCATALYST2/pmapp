/**
 * Member queries — re-export module for frontend compatibility.
 * Maps to workspace-queries which handles member queries.
 */

export {
  getWorkspaceMembers,
  getWorkspaceInvitations,
} from "./workspace-queries";
